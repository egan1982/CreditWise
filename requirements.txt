# Core dependencies
setuptools<82             # Python 3.12+ 不再内置 pkg_resources，scorecardpy 需要；>=82 移除了 pkg_resources
numpy
pandas<3.0                # pandas 3.0+ 与 scorecardpy 不兼容
pandas-stubs             # Type stubs for pandas
scikit-learn
scikit-learn-stubs       # Type stubs for scikit-learn
seaborn
matplotlib
openpyxl                 # Excel文件读写支持
python-docx              # Word文档读写支持
tqdm                     # 进度条显示

# Web UI and API
requests
websockets
aiohttp                  # 异步HTTP客户端，用于LLM SOP模式
httpx                    # HTTP客户端，用于API代理和通道测试
python-multipart
uvicorn
fastapi
openai
python-dotenv

# Scorecard and WOE/IV Analysis
scorecardpy==0.1.9.7
scipy>=1.7.0

# Feature Correlation Analysis & Advanced Statistics
statsmodels              # 偏相关、多变量回归、深度统计分析
plotly                   # 交互式可视化
kaleido==0.2.1             # Plotly图表导出为静态图片（v0.x 自带Chromium，v1.0+ 需外部Chrome）

# LLM Manager Backend - Fully integrated into DeepAnalyze
# All LLM Manager functionality (backend + frontend) is now self-contained
# Backend modules are in: llm_manager_integrated/
# Frontend is in: API/static/
# No external dependency needed!
psutil
requests-toolbelt
sqlalchemy
pydantic-settings
cryptography

# Authentication (内网多用户认证)
bcrypt                   # 密码哈希验证
pyyaml                   # 用户配置文件解析